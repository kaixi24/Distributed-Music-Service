public class ImageMetaData {
  protected final int albumId;
  protected final int imageSize;

  ImageMetaData(int albumId, int imageSize) {
    this.albumId = albumId;
    this.imageSize = imageSize;
  }
}
